// Canvas dimensions
export const CANVAS_HEIGHT = 150;
export const CANVAS_WIDTH = 200;

// Hash
export const HASH_PREVIEW_LENGTH = 16;
export const HEX_RADIX = 16;

// Image processing (RGBA pixel math)
export const RGB_CHANNEL_COUNT = 3;
export const RGBA_STRIDE = 4;

// Main thread timer
export const MAIN_TIMER_INTERVAL_MS = 100;

// Millisecond conversions
export const MS_PER_SECOND = 1000;

// Parallel workers
export const PARALLEL_DIFFICULTIES = [3, 6, 11];
export const PARALLEL_WORKER_COUNT = 3;
export const PRIMES_PER_DIFFICULTY = 80000;

// Percentage
export const PERCENTAGE_FULL = 100;

// Race bar
export const RACE_BAR_MAX_MS = 10000;

// Sort
export const SORT_RANDOM_MAX = 1000000;

// Stress test
export const STRESS_CHUNK_MS = 200;
export const STRESS_RANDOM_MAX = 999999;
export const STRESS_YIELD_MS = 5;

// Timer worker
export const TIMER_POLL_INTERVAL_MS = 50;
